{{ include('base.html.twig') }}
<body>
  <div class="jumbotron">
    <div class="container">
      <h1 class="header">🔮 WORD FREQUENCY</h1>
    </div>
  </div>
  <div class="container">
    <h1>We found:</h1>
    <ul>
      {% for item in result %}
      <li><h2>{{item}}</h2></li>

      {% endfor %}
    </ul>
    <a href="/"><button type='submit' class='btn btn-success btn-lg'>Return</button></a>
  </div>
</body>
